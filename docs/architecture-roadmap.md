# Architecture improvement plan

Baseline: 2026-09-18. This is a proposed backlog, not a claim of completed controls.
The maintainer owns decisions and operational verification; coding assistance can
prepare implementations, tests, and runbooks. Cloud changes require explicit
scope and access. No paid services are authorized by this plan.

## Priorities and acceptance evidence

| Priority | Work item | Why / acceptance evidence |
| --- | --- | --- |
| P0 | Database least privilege | Application support now separates the API reader URL from the private writer/migration URL and refuses to use a hosted Postgres writer as an API fallback. The checked-in provisioning SQL, reader-role verifier, disposable PostgreSQL CI service job, value-redacting provider-evidence preflight, manually triggered provider-audit workflow, and redacted [operator evidence packet](operator-evidence.md) builder/format prove the contract is repeatable without exposing the URL. The read-only readiness gate distinguishes blocked, stale, and ready P0 evidence without inventing provider observations, supports explicit P0/P1/full profiles for staged operational review, and can retain a profile-bound, hash-pinned decision report without reopening the packet. Still required: run the workflow against the deployed Postgres instance and retain its redacted artifact; provider-specific grants and connection limits remain operational work. |
| P0 | Restore rehearsal | A safe local SQLite rehearsal now copies into a new file, compares every forward-registry table count, and exercises the API read path. CI now adds a disposable PostgreSQL dump/restore drill, and a manually gated hosted workflow checks distinct source/target identities, empty-target safety, deterministic counts, Alembic state, the independent Go auditor, the restored read path, and a SHA-256-verifiable comparison report without uploading the dump. The redacted [operator evidence packet](operator-evidence.md) records the provider-side observation. Still required: run it against an isolated provider target and record provider-managed backup timing, RPO/RTO, and any missing data. Never rehearse by overwriting production. |
| P0 | Partial-write reconciliation | The scheduled runner has an optional post-cycle Go audit using a separate SELECT-only database role and read-only R2 token, and a manual provider-audit workflow now repeats that check independently after an outage or restore. Mirrored writes retain the verified primary reference when the mirror fails, mark the run failed without changing forecasts, and let `forward-reconcile-artifacts --repair` recover the mirror; integration tests inject the outage and retry idempotently. Provider workflows scan retained text evidence for credential-bearing values before upload, and the [operator evidence packet](operator-evidence.md) provides a secret-safe record for the required provider exercise. Still required: configure those roles, exercise the production-like object-store failure against the hosted target, and retain the provider audit/retry evidence. |
| P1 | Forward evidence inventory | Every forward attempt now emits a content-addressed manifest of declared diagnostics, status, audit, and alert files, including explicit missing-file states, safe workflow context, and a consolidated 30-day artifact. The manifest is verified before upload. GitHub artifact retention and provider-side archival remain operational controls to review. |
| P1 | Forward cache lifecycle | The scheduled runner still saves a unique resumable cache per attempt, but a separate no-secret maintenance job now uses only `actions: write` to retain the two newest `forward-runtime-*` entries and delete older entries through the GitHub cache API. The cleanup job has no repository-content permission and cannot receive registry, R2, market-data, or alert secrets. It verifies total Actions cache usage remains under the 10 GB repository quota. A cleanup failure is visible and retryable, while the cache remains an optimization rather than an evidence store. |
| P1 | Alert delivery | The runner now supports an opt-in HTTPS webhook with redacted payloads and explicit failed-run, stale-runner, registry-unavailable, quality-failure, and quality-warning classifications; delivery is non-blocking and disabled when no secret is configured. Receipts now include payload and content hashes, with a local dry-run/verifier contract. Still required: configure an agreed recipient, trigger controlled external failure/status cases, and retain delivery evidence. UI status and retained artifacts are not notification. |
| P1 | Pre-open schedule margin | Each forecast run now records the actual application-side margin from forecast recording to the next regular NYSE open. A margin below 90 minutes becomes a warning consumed by the existing status and redacted alert paths. This makes GitHub scheduler lateness visible without changing the point-in-time cutoff or the frozen model. Still required: choose an external scheduler or another operational control if the warning rate is unacceptable; application telemetry cannot make GitHub cron punctual. |
| P1 | Release gates | Use task branch → PR → green CI → self-review → merge. Branch protection is optional for this single-maintainer public repository; the lightweight PR policy is the current control. A deployment-contract validator now runs in CI and in the Vercel build, while the web job rebuilds `public/` and rejects source drift. The `.vercelignore` source-boundary contract excludes `uv.lock`, private research caches, and the operator/research tree while retaining only the reviewed snapshot-lock input and validator; the Python Function repeats the private-data exclusions and explicitly includes only the derived demo snapshot. This prevents a local research checkout from creating an oversized serving bundle. The content-addressed `config/public_snapshot.lock.json` is checked in CI and before Vercel builds so replacing the frozen v1 snapshot requires an explicit reviewed lock update. CI and the private runner also cross-validate the frozen-v1 model, locked result, selection, dataset, and SHA-256 manifest as one identity. The `deployment-smoke` workflow automatically probes a successful Production deployment status and retains a redacted 30-day report; terminal failed Production statuses (`failure`, `error`, or `inactive`) now retain a separate redacted failure artifact and fail the gate, while transient statuses and a manual HTTPS-origin trigger remain available for rechecks and non-GitHub deployments. The probe covers the homepage, disclosure files, governance contract, full security-header contract, and API health without credentials, and requires the served frozen identity in both the governance API and the provenance manifest to equal the reviewed lock at the deployed commit. CI also builds and healthchecks the provider-neutral non-root container. Provider-side deployment gating still requires account verification. |
| P1 | Frozen runtime compatibility | The private `audit_frozen_normalization.py` records the non-secret dependency runtime and reproduces the locked-test scores without writing research artifacts. `compare_frozen_runtime_reports.py` compares baseline and candidate score archives, test indices, frozen identity, and tolerances, failing closed on drift. FinBERT cache identities include PyTorch and Transformers versions, so an upgrade cannot reuse embeddings from another runtime. CI checks both tools are runnable, while the actual comparison remains operator-run because the locked dataset and scores are private. See [ADR 0021](adr/0021-frozen-runtime-compatibility-gate.md). Still required: run and review the baseline/candidate evidence before accepting a frozen-inference dependency upgrade; this gate does not authorize v1 model changes. |
| P1 | Deployment traceability | The credential-free deployment smoke probe generates a fresh correlation ID per request, requires exact `X-Request-ID` round-trip for API checks, and retains only the generated ID in redacted evidence. CDN/static checks remain compatible when they do not echo the API header. This improves incident correlation without logging response bodies, provider payloads, credentials, or arbitrary response headers. |
| P1 | Cross-domain readiness | The hash-pinned platform readiness decision composes the public-release, operator-evidence, copilot, and prospective-drift reports after their subsystem verifiers run. Missing, blocked, stale, and review-required controls remain explicit, while the aggregate carries `v1_immutable: true` and `automatic_retraining: false`; it contains no raw evidence, answer text, credentials, or provider payloads. This is an architecture-review decision aid, not a promotion or retraining gate. |
| P1 | Public surface review | The public-surface review now records the derived-data boundary, field/error exposure, bounded anonymous queries, security headers, CI/Vercel bundle validation, a machine-readable `data-provenance.json` contract, and a read-only governance endpoint that exposes the frozen identity and labels provider evidence as pending. The serving repository also verifies the content-addressed snapshot lock at runtime and fails closed on snapshot or lock drift. The `public_release_readiness` report composes the bundle and snapshot-lock validators into blocked/review-required/ready states and requires explicit approval for every source before calling the public surface distributable. Still required: confirm source/data redistribution terms for the deployed snapshot and configure provider/CDN abuse controls; the application does not pretend that anonymous serverless reads are rate-limited by Python alone. |
| P1 | Cost and capacity baseline | `edgar-moe capacity-baseline` now records local snapshot/read-path latency, registry probe latency, filesystem/cache size, disk headroom, and optional observed workflow runtime while explicitly marking provider quotas, managed DB connection counts, and object-storage usage as unobserved. Private copilot envelopes and benchmark aggregates additionally retain bounded request count, total local duration, provider-reported token counters, and peak serialized context bytes when available; unknown provider metadata is discarded. Provider transport retries are explicitly bounded to transient failures and included in request-count telemetry, while each agent run has a content-addressed aggregate wall-clock and UTF-8 context-size budget checked before every provider call, so a temporary outage, long tool loop, or growing tool history cannot create an unbounded cost/latency/provider-limit loop. Benchmark token totals remain `null` unless every successful answer reports the counter, and no billing or pricing assumption is made. The CLI keeps this operational command in the core dependency set and defers research-only imports, so the scheduled/manual hosted-capacity workflow does not install the training stack. The workflow retains a weekly runner baseline and can probe an optional public API via `EDGAR_MOE_CAPACITY_API_URL`. Still required: configure the API variable if desired, record provider measurements, and review the documented stop/approval thresholds before paid usage or larger experiments. |
| P1 | Evidence-grounded AI assistance | The optional operator-run `research-copilot` uses an OpenAI-compatible provider adapter and a bounded allowlist of read-only snapshot/registry tools. Remote HTTPS egress is additionally restricted to exact configured hostnames (default `api.openai.com`), while loopback HTTP remains the explicit local-runtime exception. Answers retain frozen identity, content-hashed citations, tool traces, explicit uncited status, a content-addressed agent policy/tool-contract identity, and optional bounded execution telemetry including peak serialized context size; live generation now binds every citation digest to the exact canonical sanitized tool payload and requested capability, while generation and offline verification fail closed when an allowlisted evidence tool was used but no tool citation survived into the final answer. Refusal-only runs retain an explicit uncited status. Envelope verification is enforced at generation, CLI write, benchmark, and evaluation boundaries, while `research-copilot-verify` checks a saved envelope before sharing or review. The provider key is never accepted by the public API or browser bundle. Operators may explicitly attach a forward diagnostic or a separately verified redacted diagnostic history; the copilot receives only validated counts, chronology, maturity, metrics, and source digests, never observations, identifiers, or private paths. Bounded `research`, `quant`, `architect`, and `operations` profiles make the same agent useful for quant, solution-architecture, and production-operations reviews; the selected profile is content-addressed in the non-secret agent identity and does not add tools or write authority. A reviewed `config/copilot_eval_cases.json` corpus, offline `research-copilot-eval` gate, bounded `research-copilot-benchmark` runner, append-only `research-copilot-review` history, and `research-copilot-readiness` gate now check expected tool use, citation sources, grounded/uncited behavior, answer-envelope safety, human rubric decisions, current-answer review coverage, and readiness without retaining answer text in aggregate reports. Benchmark aggregates explicitly classify the evaluated agent boundary as `consistent`, `legacy`, or `mixed`; only a consistent boundary can produce `ready`, while legacy/mixed runs remain `review_required`. Per-call UTF-8 context budgets fail closed before provider contact when cumulative messages/tool output grow beyond the configured 512 KiB default or 2 MiB hard maximum. Still required: run private provider reports across the corpus and accumulate reviewed history before relying on reports in research decisions; this layer never changes forecasts or replaces statistical validation. |
| P2 | Research drift observability | The `research-drift` command compares the frozen training dataset with a later prospective dataset, including missingness/distribution statistics and hash-pinned frozen component outputs. `research-drift-history` now deep-validates child hashes, one frozen baseline/model/threshold identity, chronological dataset IDs, minimum review history, aggregate status, warning counts, report references, and warning streaks; `research-drift-history-verify` rechecks a retained artifact without reopening source reports. The read-only `research-drift-readiness` gate now turns that history into a hash-pinned `ready`, `review_required`, or `blocked` decision without reading outcomes or authorizing retraining. Repeated short-horizon forward diagnostics can now also be combined into a separate redacted, content-addressed history that retains only safe counts, maturity, metrics, and source digests; raw observations and identifiers are rejected. A representative run compares `research-2026-07-31-3553e7ad78dd` (5,961 events) with `research-2026-08-06-e4ba3c323d37` (6,039 events): 807 feature columns and 9 frozen components were stable, with zero warnings or dimension mismatches. The content-hashed evidence is [`reports/research-drift-2026-08-06.json`](../reports/research-drift-2026-08-06.json), with the retained one-observation history [`reports/research-drift-history.json`](../reports/research-drift-history.json) explicitly marked `insufficient_history`. Still required: accumulate at least three later datasets/diagnostic artifacts and review retained histories; these target-free and short-horizon comparisons are not performance promotion or retraining decisions. |

P0 means highest next-work priority, not a confirmed active incident. Current role
grants, backups, provider alert settings, and billing controls are unknown until
verified. Provider allowances change; verify current account terms before any
capacity or cost decision.

## Failure scenarios to exercise

| Failure | Expected behavior | Recovery evidence |
| --- | --- | --- |
| Postgres unavailable | Forward status unavailable / data endpoints 503; historical snapshot still readable | API integration test plus isolated outage exercise |
| Source throttling or missing bars | Retry or explicit failure/quality evidence; no fabricated outcome | Fixture tests and inspectable failed run |
| Runner misses pre-entry window | Skip ineligible filings rather than backdate | Timestamp tests and zero-candidate report |
| R2 write fails after DB commit | Failed run may retain forecasts; no false atomicity claim | Injected failure and reconciler test |
| Cache disappears | Cold rebuild or visible deadline failure, never evidence loss by design | Cache-disabled staging run and runtime measurement |
| Database is lost | Documented isolated restore, then reconcile object references | Restore drill; R2 objects alone do not demonstrate full DB recovery |
| Credential exposed | Revoke/rotate affected credential and inspect use | Scoped incident checklist; no secret printed in logs or reports |

## Proposed service objectives — not measured commitments

- Integrity: zero forecasts accepted at or after their entry timestamp; verify
  every forecast path, including retries. This is an invariant, not an availability SLO.
- Freshness: preserve the existing 96-hour warning policy initially, then measure
  actual scheduled-cycle completion and pre-entry misses before tightening it.
- Recovery: start by testing a candidate RPO of 24 hours and RTO of 4 hours for
  registry loss. Do not promise either until backup cadence and restore drills
  demonstrate them. RPO is allowable lost data; RTO is time to restore service.
- Serving: gather a seven-day latency/error baseline before setting a percentile
  latency or availability target. Do not infer API health from the static homepage.

## How to use this as Solution Architecture practice

For each task, record: requirement → constraints → options → decision → failure
behavior → verification → cost → revisit condition. Explain which part is enforced
by code, which depends on infrastructure configuration, and which is only a goal.

The first application implementation is a least-privilege database role contract:
the API can receive a dedicated reader URL while the private runner retains the
writer URL. This reduces the impact of a serving-tier compromise without requiring
a new hosting provider. Production role grants, disposable Postgres tests,
migration procedure, restore/rollback steps, and an operator verification record
remain before the control can be called complete.
